#pragma once

#include <string>
#include <vector>

std::vector<std::string> read_input(const std::string&);

unsigned long part1(const std::vector<std::string>&);

unsigned long part2(const std::vector<std::string>&);
